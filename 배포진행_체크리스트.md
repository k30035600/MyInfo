# 배포 진행 체크리스트

## 1. GitHub에 최신 코드 올리기 (필수)

배포 전 변경 사항을 push 하세요.

```powershell
cd d:\OneDrive\Cursor_AI_Project\MyInfo
git add .
git status
git commit -m "chore: 배포 설정 (start_web.sh, PORT 기본값, 문서)"
git push origin main
```

---

## 2. 배포 후 확인

- 사용하는 호스팅(Heroku, Docker, 클라우드 등)에서 빌드/실행 로그 확인
- 배포 URL로 접속해 앱 동작 확인
